ACTION_SCHEMA = """
 你必须严格返回json,没有其他的任何文字.
 返回格式:

 {{
     "action": {{
         "tool": "工具名称",
         "tool_args":{{
             "参数名1": "参数值1",
             "参数名2": "参数值2"
         }}
     }}
 }}

 工具必须从提供的工具列表中选择,参数必须符合工具的要求。
 可用工具列表:
 {tools}
 """
SYSTEM_PROMPT = """
你是一个智能助手Agent,你会根据用户的需求,选择工具完成任务,只能从提供的工具中选择,不能使用其他工具。
你会:规划→思考→调用工具→反思→若未完成任务就重试重思考重新开始直到完成任务

"""
THINK_PROMPT = """
任务: {task}
历史对话: {history}
可用工具: {tools}
请思考下一步要做什么,根据下面的工具列表并输出你的思考
{action_schema}
重要指导：
1. 如果任务是简单对话（如问候、聊天），使用talk工具回复后，立即使用finish工具结束任务
2. 当你觉得需求已经完成了,请选择工具finish,并且输出{{"tool":"finish","tool_args":{{}}}}来结束任务
3. 不要重复相同的行动，如果已经回复过用户，等待用户的新输入时使用finish结束当前任务
4. 注意操作系统环境：如果shell命令执行失败，可能是Windows/Linux命令不兼容，请尝试相应的命令
5. 在Windows上创建文件可以使用：echo.>new 或 type nul>new
6. 如果任务无法完成或需要用户提供额外信息，使用finish工具结束当前任务
"""
REFLECT_PROMPT = """
你正在使用工具: {tool_name}
工具参数: {tool_args}
历史步骤:{history}
工具返回结果: {result}
请根据工具返回结果,反思:是否成功?,是否需要重试?,下一步干什么?

重要指导:
1. 如果任务已完成或无法继续进行(如需要用户输入),使用finish工具结束任务
2. 如果工具执行失败且无法修复,使用finish工具结束任务并说明原因
3. 不要重复相同的行动,如果已经请求过用户输入,使用finish工具结束当前任务
4. 如果是对话任务,回复用户后立即使用finish工具结束任务
"""
