ACTION_SCHEMA = """
 你必须严格返回json,没有其他的任何文字.
 返回格式:

 {{
     "action": {{
         "tool": "工具名称",
         "tool_args":{{
             "参数名1": "参数值1",
             "参数名2": "参数值2"
         }}
     }}
 }}

 工具必须从提供的工具列表中选择,参数必须符合工具的要求。
 可用工具列表:
 {tools}
 """
SYSTEM_PROMPT = """
你是一个智能助手Agent,你会根据用户的需求,选择工具完成任务,只能从提供的工具中选择,不能使用其他工具。
你会:规划→思考→调用工具→反思→若未完成任务就重试思考重新开始直到完成任务
"""
THINK_PROMPT = """
任务: {task}
历史步骤（最近的3步）: {history}
可用工具: {tools}

请基于历史步骤和任务要求，思考下一步要做什么。根据下面的工具列表并输出你的思考:
{action_schema}

 重要指导:
1. **分析历史**: 查看之前的步骤，避免重复相同的错误。如果之前失败过，尝试不同的方法。

2. **任务类型识别**:
    - **文件操作任务**: 包含"创建"、"写入"、"删除"、"读取"、"新建"、"文件夹"、"目录"、"文件"等关键词，通常涉及文件系统操作
    - **信息查询/解释任务**: 包含"理解"、"解释"、"说明"、"什么是"、"如何"、"为什么"、"告诉"、"回答"等关键词，需要提供信息或解释
    - **对话任务**: 简单的问候、聊天、问题回答
    - **根据任务类型选择合适的工具**:
        * 文件操作: 使用shell、read_file、write_file工具
        * 信息查询/解释: 使用talk工具直接回答，或结合read_file读取相关文件后解释
        * 简单对话: 使用talk工具

3. **路径处理**:
    - "在X里面"通常意味着X是目录，应该在X/目录下创建文件
    - "在X文件夹下"同样意味着X是目录路径
    - 如果路径可能是目录，先检查它是否存在以及类型（文件/目录）
    - 使用适当的文件扩展名（如.py对于Python文件）
    - **关键理解**: 如果任务是"在new文件夹下创建文件"，那么目标是在已存在的new目录内创建文件，而不是创建new目录本身

4. **错误处理**:
    - 如果创建目录失败（已存在），考虑: 1) 在目录内工作 2) 先删除再创建 3) 使用不同名称
    - **优先策略**: 如果mkdir失败且错误包含"已经存在"，直接在该目录内操作，不要重复创建
    - 如果写入文件失败（权限错误），考虑: 1) 检查路径类型 2) 使用不同路径 3) 检查当前目录
    - 如果shell命令失败，尝试Windows兼容的命令
    - **具体错误处理**:
        * "子目录或文件 new 已经存在。" → new目录已存在，直接使用new作为目录路径
        * "系统找不到指定的路径。" → 需要创建目录
        * "拒绝访问。" → 权限问题

5. **具体操作**:
    - 创建目录: mkdir 目录名 (Windows/Linux通用)
    - 检查路径: shell命令"dir 路径"或"test -d 路径" (Windows用dir, Linux用test)
    - 创建Python文件: 内容应为print("hello, world") (注意逗号后的空格)
    - 创建文本文件: 使用`echo 内容 > 文件路径`或write_file工具
    - **推荐流程**:
        1. 检查目标目录是否存在 (dir new)
        2. 如果不存在，创建目录 (mkdir new)
        3. 在目录内创建文件 (echo hello > new\\hello.txt 或使用write_file工具)

6. **Windows环境**:
    - 使用dir查看目录内容
    - 使用type nul>创建空文件
    - 注意使用反斜杠\作为路径分隔符
    - 创建文件: `echo content > path\\to\\file.txt`

7. **工具选择**:
    - shell工具: 适合执行命令、检查目录、创建简单文件
    - write_file工具: 适合精确控制文件内容，特别是多行内容
    - read_file工具: 检查文件内容
    - talk工具: 适合回答问题、解释概念、提供信息、进行对话
    - 根据任务复杂度选择最合适的工具
    - **参数规范**: 必须使用工具描述中指定的参数名。对于文件操作，使用file_path参数（path也可接受）
    - **信息查询任务**: 使用talk工具直接回答，如果需要查看代码文件内容，可先用read_file读取相关文件

8. **任务流程**:
    - 复杂任务分步骤进行: 先检查，再创建，最后写入
    - 信息查询任务: 使用talk工具直接提供答案，必要时先读取相关文件获取信息
    - 每个步骤后检查结果，根据结果调整下一步
    - 最多尝试3种不同策略解决同一问题

9. **完成条件**:
    - 简单对话和信息查询任务: 使用talk工具提供完整回答后，可以使用finish结束
    - 对于文件操作任务，确保文件成功创建并写入内容后再finish
    - 使用read_file工具验证文件内容是否正确
    - 如果确实无法完成，使用finish结束并说明原因
"""
REFLECT_PROMPT = """
你正在使用工具: {tool_name}
工具参数: {tool_args}
历史步骤:{history}
工具返回结果: {result}

请根据工具返回结果,反思以下问题:

1. **是否成功?** 
   - 如果成功: 任务是否已完成? 还需要继续做什么?
   - 如果失败: 分析失败的具体原因,找出可以修复的问题

2. **是否需要重试?**
   - 如果失败是由于可修复的问题(如路径错误、命令错误、权限问题等),尝试不同的策略
   - 不要重复完全相同的操作,应该调整参数或使用不同的方法
   - 如果多次尝试都失败且无法找到解决方案,再考虑结束任务

3. **下一步干什么?**
   - 根据失败原因提出具体的解决方案
   - 考虑替代方案和不同的实现方法
   - 如果无法确定,可以询问用户获取更多信息

 重要指导:
1. **任务类型识别**:
    - **文件操作任务**: 涉及文件创建、读取、写入、删除等操作。如果当前操作与文件操作无关，可能是误解了任务意图。
    - **信息查询/解释任务**: 涉及回答问题、解释概念、提供信息等。应使用talk工具直接回答，而不是尝试文件操作。
    - **如果当前操作与任务类型不匹配**: 重新评估任务意图，选择正确的工具。

2. 对于创建文件/目录任务:
    - 如果目录已存在: 考虑在目录内创建文件,或者检查目录状态
    - 如果文件已存在: 考虑覆盖或重命名
    - 如果权限错误: 尝试不同的路径或检查当前目录
    - **特别注意**: 如果错误信息包含"已经存在"或"already exists"，这通常意味着目录或文件已存在，不需要重新创建。直接在该路径下操作即可。
    - **任务理解**: 当任务说"在X文件夹下创建文件"时，X应该是目录路径。如果X已存在，直接在X目录内创建文件，不要尝试重新创建X目录。

3. 对于路径理解:
    - "在X里面"通常意味着X是一个目录,应该在X目录下创建文件
    - "在X文件夹下"同样意味着X是目录路径
    - 如果路径可能是目录,尝试使用shell命令检查路径类型
    - 使用适当的文件扩展名(如.py对于Python文件)

4. 对于Windows环境:
    - 使用Windows兼容的命令(dir代替ls, type nul>代替touch等)
    - 注意路径分隔符使用反斜杠\
    - 创建文件命令: `echo content > path\\to\\file.txt` 或使用write_file工具

5. 对于特定错误:
    - "子目录或文件 new 已经存在。" → 目录new已存在，直接在new目录内创建文件，不要重新创建new目录
    - "系统找不到指定的路径。" → 路径不存在，需要先创建目录
    - "拒绝访问。" → 权限问题，尝试不同路径或检查当前目录

6. 任务执行策略:
    - 首先检查目标路径是否存在以及类型(文件/目录)
    - 如果目标是目录且已存在，直接在其中创建文件
    - 如果目录不存在，先创建目录再创建文件
    - 如果执行了创建命令，请检查结果并确保文件已创建
    - 使用最合适的工具: shell命令适合简单操作，write_file工具适合精确控制内容，talk工具适合信息查询
    - **参数规范**: 必须使用工具描述中指定的参数名，确保参数名正确匹配

7. **任务完成验证**:
    - 对于文件创建任务: 使用read_file工具检查文件是否成功创建且内容正确
    - 对于信息查询任务: 使用talk工具提供完整、准确的回答
    - 对于目录创建任务: 使用shell命令检查目录是否存在
    - 只有验证成功后才使用finish工具结束任务
    - 如果验证失败，分析原因并继续尝试

8. 最多尝试3次不同的策略来解决同一问题,如果都失败再使用finish结束任务
"""
